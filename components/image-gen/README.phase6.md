# Image Studio Phase 6 controls

The Image Studio now supports two modes without changing its public component API:

- Generate mode: existing text-to-image flow.
- Edit mode: activated by uploading a reference image; optional mask upload is available for provider-supported inpainting.

History combines local browser entries with authenticated Supabase history. Current-preview and gallery cards expose Variation and Edit actions.
