# Phase 6 error behavior

Edit failures are surfaced directly in the Image Studio error panel and Sonner toast. The edit service does not silently substitute a text-to-image fallback when the requested provider cannot edit the source image.
